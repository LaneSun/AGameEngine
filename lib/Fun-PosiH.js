export let RatioCenter = (img,data,painter) => {    let w = img.width,        h = img.height;    if (painter.getW() / img.width > painter.getH() / img.height) {        w = painter.getH() / data.size_ratio / h * w;        h = painter.getH() / data.size_ratio;    } else {        h = painter.getW() / data.size_ratio / w * h;        w = painter.getW() / data.size_ratio;    }    return {        w: w,        h: h,        x: (painter.getW() - w) / 2,        y: (painter.getH() - h) / 2    };};export let Cells = (img,data,painter) => {    let w = painter.getW(),        h = painter.getH(),        rw = w / h > CANVAS_RATIO ? h * CANVAS_RATIO : w,        rh = w / h > CANVAS_RATIO ? h : w * CANVAS_RATIO,        sizeX = rw / data.numX,        sizeY = rh / data.numY;    return {        w: sizeX,        h: sizeY,        x: (w - rw) / 2 + sizeX * data.x,        y: (h - rh) / 2 + sizeY * data.y    };};